/* eslint-disable react/prop-types */
import "./topbarbutton.css";

const Topbarbutton = ({ title }) => {
  return (
    <>
      <button>{title}</button>
    </>
  );
};

export default Topbarbutton;
